openapi: 3.1.0
info:
  title: Api
  version: 0.1.0
  description: КадастрПро API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
  - name: auth
  - name: users
  - name: engineers
  - name: orders
  - name: bids
  - name: reviews
  - name: chat
  - name: admin
  - name: stats
  - name: verification
  - name: storage

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /auth/register:
    post:
      operationId: register
      tags: [auth]
      summary: Register a new user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResult"
        "400":
          description: Missing or invalid fields
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required: [error]
        "403":
          description: Attestat not found in Rosreestr or status is not active
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required: [error]
        "409":
          description: Attestat number already registered to another account
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required: [error]

  /auth/login:
    post:
      operationId: login
      tags: [auth]
      summary: Login
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResult"

  /auth/logout:
    post:
      operationId: logout
      tags: [auth]
      summary: Logout
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"

  /users/me:
    get:
      operationId: getMe
      tags: [users]
      summary: Get current user
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"

  /engineers:
    get:
      operationId: listEngineers
      tags: [engineers]
      summary: List engineers
      parameters:
        - name: region
          in: query
          schema:
            type: string
        - name: specialization
          in: query
          schema:
            type: string
        - name: minRating
          in: query
          schema:
            type: number
        - name: search
          in: query
          schema:
            type: string
        - name: district
          in: query
          schema:
            type: string
        - name: sro
          in: query
          schema:
            type: string
        - name: verifiedOnly
          in: query
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
        - name: limit
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EngineerList"

  /engineers/top:
    get:
      operationId: listTopEngineers
      tags: [engineers]
      summary: Top rated engineers
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Engineer"

  /engineers/me/leads:
    get:
      operationId: getMyLeads
      tags: [engineers]
      summary: Get leads for the authenticated engineer
      parameters:
        - name: page
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeadList"

  /engineers/me/balance:
    get:
      operationId: getMyBalance
      tags: [engineers]
      summary: Get billing balance for the authenticated engineer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EngineerBalance"

  /engineers/me:
    get:
      operationId: getMyEngineerProfile
      tags: [engineers]
      summary: Get current engineer profile
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Engineer"
    put:
      operationId: updateMyEngineerProfile
      tags: [engineers]
      summary: Update current engineer profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EngineerUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Engineer"

  /engineers/verify:
    post:
      operationId: verifyEngineer
      tags: [engineers]
      summary: Verify engineer registry number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VerifyInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VerifyResult"

  /engineers/{engineerId}:
    get:
      operationId: getEngineer
      tags: [engineers]
      summary: Get engineer by ID
      parameters:
        - name: engineerId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Engineer"

  /orders:
    get:
      operationId: listOrders
      tags: [orders]
      summary: List orders
      parameters:
        - name: status
          in: query
          schema:
            type: string
        - name: serviceType
          in: query
          schema:
            type: string
        - name: region
          in: query
          schema:
            type: string
        - name: customerId
          in: query
          schema:
            type: integer
        - name: page
          in: query
          schema:
            type: integer
        - name: limit
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrderList"
    post:
      operationId: createOrder
      tags: [orders]
      summary: Create order
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OrderInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Order"

  /orders/recent:
    get:
      operationId: listRecentOrders
      tags: [orders]
      summary: Recent open orders
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Order"

  /orders/{orderId}/complete:
    post:
      operationId: completeOrder
      tags: [orders]
      summary: Mark order as completed and optionally submit a review
      parameters:
        - name: orderId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CompleteOrderInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CompleteOrderResult"

  /orders/{orderId}:
    get:
      operationId: getOrder
      tags: [orders]
      summary: Get order by ID
      parameters:
        - name: orderId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Order"
    patch:
      operationId: updateOrder
      tags: [orders]
      summary: Update order
      parameters:
        - name: orderId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OrderUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Order"
    delete:
      operationId: deleteOrder
      tags: [orders]
      summary: Delete order
      parameters:
        - name: orderId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"

  /orders/{orderId}/bids:
    get:
      operationId: listOrderBids
      tags: [bids]
      summary: Get bids for an order
      parameters:
        - name: orderId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Bid"
    post:
      operationId: createBid
      tags: [bids]
      summary: Submit bid on an order
      parameters:
        - name: orderId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BidInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Bid"

  /orders/{orderId}/bids/{bidId}:
    patch:
      operationId: updateBid
      tags: [bids]
      summary: Accept or reject a bid
      parameters:
        - name: orderId
          in: path
          required: true
          schema:
            type: integer
        - name: bidId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BidUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Bid"

  /engineers/{engineerId}/bids:
    get:
      operationId: listEngineerBids
      tags: [bids]
      summary: Get bids submitted by an engineer
      parameters:
        - name: engineerId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Bid"

  /reviews:
    post:
      operationId: createReview
      tags: [reviews]
      summary: Create review
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReviewInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Review"

  /engineers/{engineerId}/reviews:
    get:
      operationId: listEngineerReviews
      tags: [reviews]
      summary: Get reviews for an engineer
      parameters:
        - name: engineerId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Review"

  /chats:
    get:
      operationId: listChats
      tags: [chat]
      summary: List chat rooms for current user
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ChatRoom"
    post:
      operationId: createChatRoom
      tags: [chat]
      summary: Create or get chat room
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChatRoomInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatRoom"

  /chats/{roomId}/read:
    post:
      operationId: markChatRead
      tags: [chat]
      summary: Mark all messages in a room as read
      parameters:
        - name: roomId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"

  /chats/{roomId}/messages:
    get:
      operationId: listMessages
      tags: [chat]
      summary: Get messages in a chat room
      parameters:
        - name: roomId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Message"
    post:
      operationId: sendMessage
      tags: [chat]
      summary: Send message
      parameters:
        - name: roomId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessageInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Message"

  /chats/{roomId}/upload:
    post:
      operationId: uploadChatFile
      tags: [chat]
      summary: Upload a file attachment for a chat room
      security:
        - bearerAuth: []
      parameters:
        - name: roomId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  description: File to upload (PDF/DOC/DOCX/XLS/XLSX/JPG/PNG/ZIP, max 20 MB)
              required: [file]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileUploadResult"
        "400":
          description: Bad request (no file or invalid type)
        "403":
          description: Not a member of this chat room

  /chats/{roomId}/complaint:
    post:
      operationId: createComplaint
      tags: [chat]
      summary: Submit a complaint about a chat room
      parameters:
        - name: roomId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ComplaintInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Complaint"

  /admin/complaints:
    get:
      operationId: listAdminComplaints
      tags: [admin]
      summary: List all complaints (admin)
      parameters:
        - name: status
          in: query
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ComplaintList"

  /admin/complaints/{complaintId}/resolve:
    patch:
      operationId: resolveComplaint
      tags: [admin]
      summary: Resolve a complaint (admin)
      parameters:
        - name: complaintId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Complaint"

  /storage/uploads/request-url:
    post:
      operationId: requestUploadUrl
      tags: [storage]
      summary: Request a presigned URL for file upload
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UploadUrlRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadUrlResponse"

  /storage/objects/{objectPath}:
    get:
      operationId: getStorageObject
      tags: [storage]
      summary: Serve a stored object
      parameters:
        - name: objectPath
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Object content

  /stats/summary:
    get:
      operationId: getStatsSummary
      tags: [stats]
      summary: Platform stats summary
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatsSummary"

  /admin/stats:
    get:
      operationId: getAdminStats
      tags: [admin]
      summary: Admin dashboard stats
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminStats"

  /admin/users:
    get:
      operationId: listAdminUsers
      tags: [admin]
      summary: List all users (admin)
      parameters:
        - name: role
          in: query
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserList"

  /admin/users/{userId}:
    patch:
      operationId: updateAdminUser
      tags: [admin]
      summary: Update user (admin)
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminUserUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"

  /admin/orders:
    get:
      operationId: listAdminOrders
      tags: [admin]
      summary: List all orders (admin)
      parameters:
        - name: status
          in: query
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrderList"

  /settings:
    get:
      operationId: getSettings
      tags: [engineers]
      summary: Get public platform settings (debt_limit, boost prices)
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformSettings"

  /admin/settings:
    get:
      operationId: getAdminSettings
      tags: [admin]
      summary: Get platform settings (debt limit, boost prices, etc.)
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformSettings"
    put:
      operationId: updateAdminSettings
      tags: [admin]
      summary: Update platform settings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PlatformSettingsInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformSettings"

  /admin/lead-prices:
    get:
      operationId: getAdminLeadPrices
      tags: [admin]
      summary: Get configured lead prices per service type
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LeadPrice"
    put:
      operationId: updateAdminLeadPrices
      tags: [admin]
      summary: Upsert lead prices
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LeadPricesInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LeadPrice"

  /admin/leads:
    get:
      operationId: listAdminLeads
      tags: [admin]
      summary: List leads (admin)
      parameters:
        - name: engineerId
          in: query
          schema:
            type: integer
        - name: paymentStatus
          in: query
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeadList"

  /admin/leads/summary:
    get:
      operationId: getAdminLeadsSummary
      tags: [admin]
      summary: Per-engineer debt summary
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EngineerDebtSummary"

  /admin/leads/{leadId}:
    patch:
      operationId: updateAdminLead
      tags: [admin]
      summary: Mark lead as paid / unpaid
      parameters:
        - name: leadId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LeadUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Lead"

  /admin/engineers:
    get:
      operationId: listAdminEngineers
      tags: [admin]
      summary: List engineers with billing info (admin)
      parameters:
        - name: page
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminEngineerList"

  /admin/users/{userId}/role:
    put:
      operationId: setUserRole
      tags: [admin]
      summary: Change user role (superadmin only)
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserRoleUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"

  /admin/engineers/{engineerId}/visibility:
    patch:
      operationId: setEngineerVisibility
      tags: [admin]
      summary: Show or hide an engineer profile (admin)
      parameters:
        - name: engineerId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EngineerVisibilityUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EngineerVisibilityResult"

  /admin/engineers/{engineerId}:
    delete:
      operationId: deleteAdminEngineer
      tags: [admin]
      summary: Delete engineer profile (admin)
      parameters:
        - name: engineerId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
    patch:
      operationId: updateAdminEngineer
      tags: [admin]
      summary: Set PRO status or apply profile boost (admin)
      parameters:
        - name: engineerId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminEngineerUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminEngineerResult"

  /admin/reviews:
    get:
      operationId: listAdminReviews
      tags: [admin]
      summary: List all reviews for moderation (admin)
      parameters:
        - name: moderationStatus
          in: query
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminReviewList"

  /admin/reviews/{reviewId}:
    patch:
      operationId: moderateReview

      tags: [admin]
      summary: Publish or hide a review (admin)
      parameters:
        - name: reviewId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminReviewUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminReviewItem"

  /admin/verification-logs:
    get:
      operationId: listAdminVerificationLogs
      tags: [admin]
      summary: List all verification logs (admin)
      parameters:
        - name: page
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VerificationLogList"

  /admin/engineers/{id}/reverify:
    post:
      operationId: reverifyEngineer
      tags: [admin]
      summary: Re-run Rosreestr verification for an engineer (admin)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReverifyResult"

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required: [status]

    MessageResponse:
      type: object
      properties:
        message:
          type: string
      required: [message]

    User:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        email:
          type: string
        role:
          type: string
        phone:
          type: ["string", "null"]
        telegram:
          type: ["string", "null"]
        whatsapp:
          type: ["string", "null"]
        avatarUrl:
          type: ["string", "null"]
        isBlocked:
          type: ["string", "null"]
        createdAt:
          type: string
      required: [id, name, email, role, createdAt]

    UserList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/User"
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer
      required: [items, total, page, limit]

    RegisterInput:
      type: object
      properties:
        name:
          type: string
        email:
          type: string
        password:
          type: string
        role:
          type: string
        phone:
          type: string
        attestatNumber:
          type: string
      required: [name, email, password, role]

    LoginInput:
      type: object
      properties:
        email:
          type: string
        password:
          type: string
      required: [email, password]

    AuthResult:
      type: object
      properties:
        user:
          $ref: "#/components/schemas/User"
        token:
          type: string
      required: [user, token]

    PortfolioItem:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        type:
          type: string
        region:
          type: string
        year:
          type: integer
        area:
          type: ["string", "null"]
      required: [title, description, type, region, year]

    ProfileBoost:
      type: object
      properties:
        id:
          type: integer
        engineerId:
          type: integer
        period:
          type: integer
        expiresAt:
          type: string
        createdAt:
          type: string
      required: [id, engineerId, period, expiresAt, createdAt]

    Engineer:
      type: object
      properties:
        id:
          type: integer
        userId:
          type: integer
        user:
          $ref: "#/components/schemas/User"
        registryNumber:
          type: string
        specializations:
          type: array
          items:
            type: string
        region:
          type: string
        regions:
          type: array
          items:
            type: string
        district:
          type: ["string", "null"]
        sro:
          type: ["string", "null"]
        experience:
          type: integer
        bio:
          type: ["string", "null"]
        isVerified:
          type: boolean
        isOnline:
          type: boolean
        isHidden:
          type: boolean
        rating:
          type: number
        reviewCount:
          type: integer
        completedOrders:
          type: integer
        responseTime:
          type: ["string", "null"]
        priceFrom:
          type: ["integer", "null"]
        portfolioItems:
          type: array
          items:
            $ref: "#/components/schemas/PortfolioItem"
        isPro:
          type: boolean
        proExpiresAt:
          type: ["string", "null"]
        debtAmount:
          type: integer
        createdAt:
          type: string
        attestatNumber:
          type: ["string", "null"]
        rosreestrStatus:
          type: ["string", "null"]
        sroName:
          type: ["string", "null"]
        rosreestrCheckedAt:
          type: ["string", "null"]
        rosreestrWorksCount:
          type: ["integer", "null"]
        rosreestrRejectionsCount:
          type: ["integer", "null"]
        rosreestrSuspensionsCount:
          type: ["integer", "null"]
        rosreestrRejectionRate:
          type: ["number", "null"]
      required: [id, userId, user, registryNumber, specializations, region, regions, experience, isVerified, isOnline, isHidden, rating, reviewCount, completedOrders, portfolioItems, isPro, debtAmount, createdAt]

    EngineerList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Engineer"
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer
      required: [items, total, page, limit]

    EngineerUpdate:
      type: object
      properties:
        specializations:
          type: array
          items:
            type: string
        region:
          type: string
        regions:
          type: array
          items:
            type: string
        experience:
          type: integer
        bio:
          type: string
        phone:
          type: string
        telegram:
          type: string
        whatsapp:
          type: string
        responseTime:
          type: string
        priceFrom:
          type: integer
        isOnline:
          type: boolean
        portfolioItems:
          type: array
          items:
            $ref: "#/components/schemas/PortfolioItem"

    VerifyInput:
      type: object
      properties:
        registryNumber:
          type: string
      required: [registryNumber]

    VerifyResult:
      type: object
      properties:
        isValid:
          type: boolean
        engineerName:
          type: ["string", "null"]
        message:
          type: string
      required: [isValid, message]

    Order:
      type: object
      properties:
        id:
          type: integer
        customerId:
          type: integer
        customer:
          $ref: "#/components/schemas/User"
        title:
          type: string
        description:
          type: string
        serviceType:
          type: string
        region:
          type: string
        budget:
          type: ["number", "null"]
        deadline:
          type: ["string", "null"]
        status:
          type: string
        bidCount:
          type: integer
        createdAt:
          type: string
      required: [id, customerId, customer, title, description, serviceType, region, status, bidCount, createdAt]

    OrderList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Order"
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer
      required: [items, total, page, limit]

    OrderInput:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        serviceType:
          type: string
        region:
          type: string
        budget:
          type: number
        deadline:
          type: string
        asDraft:
          type: boolean
      required: [title, description, serviceType, region]

    CompleteOrderInput:
      type: object
      properties:
        engineerId:
          type: integer
        rating:
          type: integer
        comment:
          type: string
      required: [engineerId]

    CompleteOrderResult:
      type: object
      properties:
        order:
          $ref: "#/components/schemas/Order"
        review:
          $ref: "#/components/schemas/Review"
      required: [order]

    OrderUpdate:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        status:
          type: string
        budget:
          type: number
        deadline:
          type: string

    Bid:
      type: object
      properties:
        id:
          type: integer
        orderId:
          type: integer
        order:
          $ref: "#/components/schemas/Order"
        engineerId:
          type: integer
        engineer:
          $ref: "#/components/schemas/Engineer"
        message:
          type: string
        price:
          type: ["number", "null"]
        proposedDeadline:
          type: ["string", "null"]
        status:
          type: string
        createdAt:
          type: string
      required: [id, orderId, order, engineerId, engineer, message, status, createdAt]

    BidInput:
      type: object
      properties:
        message:
          type: string
        price:
          type: number
        proposedDeadline:
          type: string
      required: [message]

    BidUpdate:
      type: object
      properties:
        status:
          type: string
      required: [status]

    Review:
      type: object
      properties:
        id:
          type: integer
        orderId:
          type: integer
        authorId:
          type: integer
        author:
          $ref: "#/components/schemas/User"
        engineerId:
          type: integer
        rating:
          type: integer
        comment:
          type: ["string", "null"]
        moderationStatus:
          type: string
        createdAt:
          type: string
      required: [id, orderId, authorId, author, engineerId, rating, moderationStatus, createdAt]

    ReviewInput:
      type: object
      properties:
        orderId:
          type: integer
        engineerId:
          type: integer
        rating:
          type: integer
        comment:
          type: string
      required: [orderId, engineerId, rating]

    ChatRoom:
      type: object
      properties:
        id:
          type: integer
        orderId:
          type: ["integer", "null"]
        order:
          $ref: "#/components/schemas/Order"
        customerId:
          type: integer
        customer:
          $ref: "#/components/schemas/User"
        engineerId:
          type: integer
        engineer:
          $ref: "#/components/schemas/Engineer"
        lastMessage:
          type: ["string", "null"]
        lastMessageAt:
          type: ["string", "null"]
        unreadCount:
          type: integer
        createdAt:
          type: string
      required: [id, customerId, customer, engineerId, engineer, unreadCount, createdAt]

    ChatRoomInput:
      type: object
      properties:
        engineerId:
          type: integer
        orderId:
          type: integer
      required: [engineerId]

    Message:
      type: object
      properties:
        id:
          type: integer
        roomId:
          type: integer
        senderId:
          type: integer
        sender:
          $ref: "#/components/schemas/User"
        text:
          type: string
        isRead:
          type: boolean
        attachmentUrl:
          type: ["string", "null"]
        attachmentName:
          type: ["string", "null"]
        attachmentType:
          type: ["string", "null"]
        createdAt:
          type: string
      required: [id, roomId, senderId, sender, text, isRead, createdAt]

    MessageInput:
      type: object
      properties:
        text:
          type: string
        attachmentUrl:
          type: string
        attachmentName:
          type: string
        attachmentType:
          type: string

    FileUploadResult:
      type: object
      properties:
        url:
          type: string
        name:
          type: string
        type:
          type: string
        size:
          type: integer
      required: [url, name, type]

    Complaint:
      type: object
      properties:
        id:
          type: integer
        roomId:
          type: integer
        reporterId:
          type: integer
        reporterName:
          type: ["string", "null"]
        description:
          type: string
        status:
          type: string
        createdAt:
          type: string
        resolvedAt:
          type: ["string", "null"]
      required: [id, roomId, reporterId, description, status, createdAt]

    ComplaintInput:
      type: object
      properties:
        description:
          type: string
      required: [description]

    ComplaintList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Complaint"
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer
      required: [items, total, page, limit]

    UploadUrlRequest:
      type: object
      properties:
        name:
          type: string
        size:
          type: integer
        contentType:
          type: string
      required: [name, size, contentType]

    UploadUrlResponse:
      type: object
      properties:
        uploadURL:
          type: string
        objectPath:
          type: string
        metadata:
          type: object
          properties:
            name:
              type: string
            size:
              type: integer
            contentType:
              type: string
      required: [uploadURL, objectPath]

    StatsSummary:
      type: object
      properties:
        totalEngineers:
          type: integer
        verifiedEngineers:
          type: integer
        totalOrders:
          type: integer
        completedOrders:
          type: integer
        totalCustomers:
          type: integer
      required: [totalEngineers, verifiedEngineers, totalOrders, completedOrders, totalCustomers]

    AdminStats:
      type: object
      properties:
        totalUsers:
          type: integer
        totalEngineers:
          type: integer
        totalCustomers:
          type: integer
        totalOrders:
          type: integer
        openOrders:
          type: integer
        completedOrders:
          type: integer
        verifiedEngineers:
          type: integer
        pendingReviews:
          type: integer
        totalRevenue:
          type: number
        newUsersThisMonth:
          type: integer
      required: [totalUsers, totalEngineers, totalCustomers, totalOrders, openOrders, completedOrders, verifiedEngineers, pendingReviews, totalRevenue, newUsersThisMonth]

    AdminUserUpdate:
      type: object
      properties:
        role:
          type: string
        isBlocked:
          type: boolean

    PlatformSettings:
      type: object
      additionalProperties:
        type: string
      description: Key-value map of platform settings (debt_limit, boost_price_7d, boost_price_30d, boost_price_90d)

    PlatformSettingsInput:
      type: object
      properties:
        settings:
          type: object
          additionalProperties:
            type: string
      required: [settings]

    EngineerBalance:
      type: object
      properties:
        debtAmount:
          type: integer
        totalAccrued:
          type: integer
        totalPaid:
          type: integer
        leadCount:
          type: integer
      required: [debtAmount, totalAccrued, totalPaid, leadCount]

    LeadPrice:
      type: object
      properties:
        id:
          type: integer
        serviceType:
          type: string
        price:
          type: integer
        updatedAt:
          type: string
      required: [id, serviceType, price, updatedAt]

    LeadPricesInput:
      type: object
      properties:
        prices:
          type: array
          items:
            type: object
            properties:
              serviceType:
                type: string
              price:
                type: integer
            required: [serviceType, price]
      required: [prices]

    Lead:
      type: object
      properties:
        id:
          type: integer
        orderId:
          type: integer
        engineerId:
          type: integer
        engineerName:
          type: string
        serviceType:
          type: string
        leadCost:
          type: integer
        paymentStatus:
          type: string
        createdAt:
          type: string
      required: [id, orderId, engineerId, serviceType, leadCost, paymentStatus, createdAt]

    LeadList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Lead"
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer
      required: [items, total, page, limit]

    EngineerDebtSummary:
      type: object
      properties:
        engineerId:
          type: integer
        engineerName:
          type: string
        debtAmount:
          type: integer
        totalAccrued:
          type: integer
        totalPaid:
          type: integer
        leadCount:
          type: integer
      required: [engineerId, engineerName, debtAmount, totalAccrued, totalPaid, leadCount]

    LeadUpdate:
      type: object
      properties:
        paymentStatus:
          type: string
      required: [paymentStatus]

    AdminEngineerItem:
      type: object
      properties:
        id:
          type: integer
        userId:
          type: integer
        name:
          type: string
        email:
          type: string
        region:
          type: string
        rating:
          type: number
        isVerified:
          type: boolean
        isHidden:
          type: boolean
        isPro:
          type: boolean
        proExpiresAt:
          type: ["string", "null"]
        debtAmount:
          type: integer
        sroName:
          type: ["string", "null"]
        attestatNumber:
          type: ["string", "null"]
        rosreestrRejectionRate:
          type: ["number", "null"]
        specializations:
          type: array
          items:
            type: string
        activeBoost:
          $ref: "#/components/schemas/ProfileBoost"
      required: [id, userId, name, email, region, rating, isVerified, isHidden, isPro, debtAmount, specializations]

    AdminEngineerList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/AdminEngineerItem"
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer
      required: [items, total, page, limit]

    AdminEngineerUpdate:
      type: object
      properties:
        isPro:
          type: boolean
        proExpiresAt:
          type: ["string", "null"]
        boostPeriod:
          type: integer
        isHidden:
          type: boolean

    AdminEngineerResult:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        isPro:
          type: boolean
        isHidden:
          type: boolean
        proExpiresAt:
          type: ["string", "null"]
        debtAmount:
          type: integer
        activeBoost:
          $ref: "#/components/schemas/ProfileBoost"
      required: [id, name, isPro, isHidden, debtAmount]

    UserRoleUpdate:
      type: object
      properties:
        role:
          type: string
      required: [role]

    EngineerVisibilityUpdate:
      type: object
      properties:
        isHidden:
          type: boolean
      required: [isHidden]

    EngineerVisibilityResult:
      type: object
      properties:
        id:
          type: integer
        isHidden:
          type: boolean
      required: [id, isHidden]

    AdminReviewItem:
      type: object
      properties:
        id:
          type: integer
        orderId:
          type: integer
        authorId:
          type: integer
        authorName:
          type: string
        authorEmail:
          type: string
        engineerId:
          type: integer
        engineerName:
          type: string
        rating:
          type: integer
        comment:
          type: ["string", "null"]
        moderationStatus:
          type: string
        createdAt:
          type: string
      required: [id, orderId, authorId, authorName, authorEmail, engineerId, engineerName, rating, moderationStatus, createdAt]

    AdminReviewList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/AdminReviewItem"
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer
      required: [items, total, page, limit]

    AdminReviewUpdate:
      type: object
      properties:
        moderationStatus:
          type: string
      required: [moderationStatus]

    VerificationLog:
      type: object
      properties:
        id:
          type: integer
        engineerId:
          type: ["integer", "null"]
        engineerName:
          type: ["string", "null"]
        engineerEmail:
          type: ["string", "null"]
        attestatNumber:
          type: string
        result:
          type: string
        failureReason:
          type: ["string", "null"]
        rawSnapshot:
          type: ["string", "null"]
        checkedAt:
          type: string
      required: [id, attestatNumber, result, checkedAt]

    VerificationLogList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/VerificationLog"
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer
      required: [items, total, page, limit]

    ReverifyResult:
      type: object
      properties:
        isValid:
          type: boolean
        message:
          type: string
      required: [isValid, message]
